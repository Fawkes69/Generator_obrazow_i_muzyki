name: aplikacja_gen_obraz_np
description: "ACAB"
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  just_audio: ^0.9.35
  path_provider: ^2.1.0
  image: ^4.1.4
  flutter_colorpicker: ^1.0.3
  vector_math: ^2.1.4
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
    - assets/images/